{% set courses = craft.entries.section("courses").all() %}
<h1 class="articleList__header">{{entry.title}}</h1>
<div class="courseList__container">
    {% for course in courses %}
        {% include "_content/parts/_course.twig" %}
    {% endfor %}
</div>